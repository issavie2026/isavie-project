# Public assets

Copy the project logo here so the header displays it:

```bash
cp ../logo/Photoroom_20260103_130025.png logo.png
```

(from the `frontend` directory, or from repo root: `cp logo/Photoroom_20260103_130025.png frontend/public/logo.png`)
