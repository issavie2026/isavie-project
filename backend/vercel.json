{
  "buildCommand": "npm run vercel-build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/[...all]"
    }
  ],
  "functions": {
    "api/[...all].js": {
      "maxDuration": 30
    }
  }
}
