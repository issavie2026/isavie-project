DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public"
JWT_SECRET="change-me-in-production-min-32-chars"
MAGIC_LINK_SECRET="change-me-magic-link-secret-32chars"
INVITE_SECRET="change-me-invite-secret-32chars"
FRONTEND_URL="http://localhost:5173"
PORT="3004"
SMTP_URL=""
SMTP_FROM="Issavie <no-reply@issavie.com>"
RESEND_API_KEY=""
UNLOCK_ALL="false"
# Optional: SMTP_URL for real email (e.g. smtp://user:pass@smtp.example.com:587). Leave empty to log magic links to console.
# Optional: RESEND_API_KEY for Resend API delivery. If set, it is preferred over SMTP_URL.
# Optional: FRONTEND_URL can be a comma-separated allowlist for CORS.
# Optional: set UNLOCK_ALL="true" for local testing to bypass feature and role gates.
